spring:
 application:
  name: sgm-api-gateway-zuul

 cloud:
  config:
   uri: http://sgm-config-server.herokuapp.com/api/v1

 profiles:
  active: prod

# Actuator (Atualizar as configurações que pegamos do config server em tempo de execução)
management:
 endpoints:
  web:
   exposure:
    include: "*"